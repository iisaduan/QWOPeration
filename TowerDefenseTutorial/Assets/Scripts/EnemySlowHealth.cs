using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemySlowHealth : Enemy
{
    /*
    public override void TakeDamage(float damage)
    {
        speed += health * .6f ;
        health -= damage;

        healthBar.fillAmount = health / startHealth;

        if (health <= 0)
        {
            Die();
        }
    } */
}
